import { Events, EventTypes } from './types';
export declare const eventMatrix: {
    name: keyof typeof EventTypes;
    enabled: boolean;
    test: boolean;
    events: {
        [key in Events]: string;
    };
}[];
//# sourceMappingURL=eventMatrix.d.ts.map